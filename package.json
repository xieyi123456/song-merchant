{
  "name": "song-merchant",
  "version": "0.1.0",
  "private": true,
  "description": "大宋百商图 线上桌游",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@song-merchant/shared && npm run build --workspace=@song-merchant/server && npm run build --workspace=@song-merchant/client",
    "test": "npm run test --workspace=packages/shared",
    "dev:client": "npm run dev --workspace=packages/client",
    "dev:server": "npm run dev --workspace=packages/server",
    "typecheck": "npm run typecheck --workspaces"
  }
}
